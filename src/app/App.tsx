import { Container, Typography } from '@mui/material';

export function App() {
  return (
    <Container maxWidth="md" sx={{ mt: 4 }}>
      <Typography variant="h4">Gestor de Tareas ICE</Typography>
    </Container>
  );
}
